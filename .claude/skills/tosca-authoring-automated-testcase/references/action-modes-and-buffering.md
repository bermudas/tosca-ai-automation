# Action modes and buffering

When editing automated test step values in Cloud Builder:

| Mode | Purpose |
|------|---------|
| Input | Set value to send to application |
| Verify | Assert expected value |
| WaitOn | Wait for condition |
| Buffer | Store value for later steps |

Apply via Builder update APIs after scaffolding. Match conventions from existing modules in the workspace.
